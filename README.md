# Docker Build CI Project

This project demonstrates a simple GitHub Actions CI pipeline
that automatically builds and runs a Docker container on every push.

## Tech Used
- Docker
- GitHub Actions
- Python

## CI Workflow
On every push to the main branch:
1. The Docker image is built
2. The container is executed
3. The workflow verifies successful execution

